# SELinux Hands-On

## Lamp

Installed on Debian for testing purposes

`simple-lamp` folder contains all necessary files for LAMP webserver

Use `lamp.sh` for installation and `simple.conf` to configure the webserver 
